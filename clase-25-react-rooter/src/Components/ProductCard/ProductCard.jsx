import React from 'react'

const ProductCard = ({producto}) => {
    const {nombre,
        descripcion,
        precio,
        id,
        stock,
        codigo,
        categoria} = producto
  return (
    <div>
      
    </div>
  )
}

export default ProductCard
